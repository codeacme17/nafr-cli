const COLORS = {
  RED: "#EF4444",
  GREEN: "#10B981",
  YELLOW: "#FCD34D",
}

const PLUGIN = ["axios", "tailwindcss", "eslint"]

const PROJECT = ["vanilla", "vue", "react"]

module.exports = {
  COLORS,
  PLUGIN,
  PROJECT,
}
